// src/features/sequencer/components/SequencerVisualisation/PianoRollVisualisation.tsx

interface PianoRollVisualisationProps {
  trackId: string;
  patternId: string;
}

const PianoRollVisualisation: React.FC<PianoRollVisualisationProps> = ({
  trackId,
  patternId,
}) => {
  return <>Piano Roll Visualisation</>;
};

export default PianoRollVisualisation;
