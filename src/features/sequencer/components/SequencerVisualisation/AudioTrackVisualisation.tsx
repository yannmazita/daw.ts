// src/features/sequencer/components/SequencerVisualisation/AudioTrackVisualisation.tsx

interface AudioTrackVisualisationProps {
  trackId: string;
  patternId: string;
}

const AudioTrackVisualisation: React.FC<AudioTrackVisualisationProps> = ({
  trackId,
  patternId,
}) => {
  return <>Audio Track Visualisation</>;
};

export default AudioTrackVisualisation;
