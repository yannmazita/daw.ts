// src/common/middle/instrumentManagerInstance.ts

import { InstrumentManager } from "./InstrumentManager";

/**
 * Singleton instance of InstrumentManager to be used throughout the application.
 */
export const instrumentManager = new InstrumentManager();
